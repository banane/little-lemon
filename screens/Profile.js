import { Text, View } from 'react-native';

const Profile = ({navigation}) => {
    return (
        <View style={{alignItems: 'center',}}>
            <Text style={{marginTop: 100,}}>Profile view</Text>
        </View>
    )
};

export default Profile;