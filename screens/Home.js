import { View} from 'react-native';
import MenuList from '../components/MenuList';

const HomeScreen = () => {
   

    return(
        <View><MenuList /></View>
        
    );
};

export default HomeScreen;
